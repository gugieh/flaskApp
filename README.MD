**At first install this:**

pip install Flask
pip install Flask-SQLAlchemy
pip install Flask-Login
pip install Flask-WTF
pip install WTForms
pip install Flask-Bcrypt
pip install Flask-Bootstrap
pip install flask-bs4

**Later**   

python app.py